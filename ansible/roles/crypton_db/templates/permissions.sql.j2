begin;

grant all on account to {{ item.db.user|default(item.user) }};
grant all on base_keyring to {{ item.db.user|default(item.user) }};
grant all on commitable_transaction to {{ item.db.user|default(item.user) }};
grant all on container to {{ item.db.user|default(item.user) }};
grant all on container_record to {{ item.db.user|default(item.user) }};
grant all on container_session_key to {{ item.db.user|default(item.user) }};
grant all on container_session_key_share to {{ item.db.user|default(item.user) }};
grant all on first_pending_commit_per_avatar to {{ item.db.user|default(item.user) }};
grant all on message to {{ item.db.user|default(item.user) }};
grant all on readable_container_records_by_account to {{ item.db.user|default(item.user) }};
grant all on transaction to {{ item.db.user|default(item.user) }};
grant all on transaction_add_container to {{ item.db.user|default(item.user) }};
grant all on transaction_add_container_record to {{ item.db.user|default(item.user) }};
grant all on transaction_add_container_session_key to {{ item.db.user|default(item.user) }};
grant all on transaction_add_container_session_key_share to {{ item.db.user|default(item.user) }};
grant all on transaction_add_message to {{ item.db.user|default(item.user) }};
grant all on transaction_delete_container to {{ item.db.user|default(item.user) }};
grant all on transaction_delete_container_session_key_share to {{ item.db.user|default(item.user) }};
grant all on transaction_delete_message to {{ item.db.user|default(item.user) }};
grant all on version_identifier to {{ item.db.user|default(item.user) }};

commit;
